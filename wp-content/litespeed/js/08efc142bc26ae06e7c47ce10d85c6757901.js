document.addEventListener("DOMContentLoaded",function(){const section=document.querySelector(".custom-funds");if(!section)return;const tabs=section.querySelectorAll(".tab");const funds=section.querySelectorAll(".fund-container");tabs.forEach((tab)=>{tab.addEventListener("click",()=>{const selectedTagID=tab.getAttribute("data-id");funds.forEach((fund)=>{const fundTagID=fund.getAttribute("data-id");const match=fundTagID===selectedTagID||selectedTagID==-1;fund.classList.toggle("d-none",!match)});tabs.forEach((tab)=>{tab.classList.remove("active")});tab.classList.add("active")})});const firstTab=tabs[0];if(firstTab){firstTab.click()}})
;