document.addEventListener("DOMContentLoaded",()=>{const mapButtons=document.querySelectorAll(".map-tabs button");const mapIframe=document.getElementById("map");const googleAnchor=document.querySelector(".map-descrpition a");const addressElement=document.querySelector(".map-descrpition p");mapButtons.forEach((button)=>{button.addEventListener("click",()=>{mapButtons.forEach((btn)=>btn.classList.remove("active"));button.classList.add("active");const mapSrc=button.getAttribute("data-map");const googleHref=button.getAttribute("data-google");const address=button.getAttribute("data-address");mapIframe.src=mapSrc;googleAnchor.href=googleHref;addressElement.textContent=address})})})
;